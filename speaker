local s = peripheral.find("speaker")
rednet.open("left")
print("basst")
 
while true do
  local sender, message, protocol = rednet.receive()
 
  if message == "stop" then
    print("")
    s.stop()
  else
    s.playSound(message)
    print("Message")
  end 
end
